using '../main.bicep'

param appServiceSkuName                         = readEnvironmentVariable('APP_SERVICE_SKU_NAME')
param chatGptDeploymentName                     = readEnvironmentVariable('CHAT_GPT_DEPLOYMENT_NAME')
param documentIntelligenceResourceGroupLocation = readEnvironmentVariable('DOCUMENT_INTELLIGENCE_RESOURCE_GROUP_LOCATION')
param embeddingDeploymentName                   = readEnvironmentVariable('EMBEDDING_DEPLOYMENT_NAME')
param environmentName                           = readEnvironmentVariable('AZURE_ENV_NAME')
param location                                  = readEnvironmentVariable('AZURE_LOCATION')
param openAiHost                                = readEnvironmentVariable('OPENAI_HOST')
param openAiResourceGroupLocation               = readEnvironmentVariable('OPENAI_RESOURCE_GROUP_LOCATION')
param searchIndexName                           = readEnvironmentVariable('SEARCH_INDEX_NAME')
param searchQueryLanguage                       = readEnvironmentVariable('SEARCH_QUERY_LANGUAGE')
param searchQuerySpeller                        = readEnvironmentVariable('SEARCH_QUERY_SPELLER')
param searchServiceSemanticRankerLevel          = readEnvironmentVariable('SEARCH_SERVICE_SEMANTIC_RANKER_LEVEL')
param searchServiceSkuName                      = readEnvironmentVariable('SEARCH_SERVICE_SKU_NAME')
param storageSkuName                            = readEnvironmentVariable('STORAGE_SKU_NAME')

//param environmentName = readEnvironmentVariable('AZURE_ENV_NAME')
//param location = readEnvironmentVariable('AZURE_LOCATION')
param useVirtualNetworkIntegration = bool(readEnvironmentVariable('USE_VIRTUAL_NETWORK_INTEGRATION', 'false'))
param useVirtualNetworkPrivateEndpoint = bool(readEnvironmentVariable('USE_VIRTUAL_NETWORK_PRIVATE_ENDPOINT', 'false'))
param virtualNetworkAddressSpacePrefix = readEnvironmentVariable('VIRTUAL_NETWORK_ADDRESS_SPACE_PREFIX', '10.1.0.0/16')
param virtualNetworkIntegrationSubnetAddressSpacePrefix = readEnvironmentVariable('VIRTUAL_NETWORK_INTEGRATION_SUBNET_ADDRESS_SPACE_PREFIX', '10.1.1.0/24')
param virtualNetworkPrivateEndpointSubnetAddressSpacePrefix = readEnvironmentVariable('VIRTUAL_NETWORK_PRIVATE_ENDPOINT_SUBNET_ADDRESS_SPACE_PREFIX', '10.1.2.0/24')
param principalId = readEnvironmentVariable('AZURE_PRINCIPAL_ID', '')
//param principalType = readEnvironmentVariable('AZURE_PRINCIPAL_TYPE', 'User')
